<%= form_for(@team, :url => hackathon_teams_path) do |f| %>
  <% if @team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team.errors.count, "error") %> prohibited this team from being saved:</h2>

      <ul>
      <% @team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field span8">
    <%= f.label "Team Name" %><br />
    <%= f.text_field :name, :class => "span11" %>
  </div>

  <div class="field span8">
    <%= f.label "Team Project (optional)" %><br />
    <%= f.text_area :project, :class => "span11" %>
  </div>

  <% if @current_teammates %>
    Current Teammates:
    <ul>
      <% @current_teammates.each do |user| %>
        <li><%= link_to(user.name, user_path(user)) %></li>
      <% end %>
    </ul>
  <% else %>
    No teammates yet
  <% end %>

  <%= f.label "Add Teammates" %>
  <select name="teammates[]">
    <%= options_from_collection_for_select(@potential_teammates, :id, :name) %>
  </select> 
  <br>
  <select name="teammates[]">
    <%= options_from_collection_for_select(@potential_teammates, :id, :name) %>
  </select>
  <br>
  <select name="teammates[]">
    <%= options_from_collection_for_select(@potential_teammates, :id, :name) %>
  </select>

  
  <!-- <div class="field">
    <%#= f.label "What other type of people with the following skills would you like to have join your team?" %><br />
    <%#= f.text_field :email %>
  </div> -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
